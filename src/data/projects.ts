import type { Project } from '@/types'

export const projects: Project[] = [
  {
    slug: 'makemore',
    title: 'Makemore',
    description: 'Character-level name generation model using a WaveNet-style hierarchical MLP trained on 32k names.',
    year: '2026',
    tags: ['Python', 'PyTorch', 'ML'],
    status: 'complete',
    overview: 'A character-level language model that generates plausible new names by predicting one character at a time. Built in PyTorch following Karpathy\'s makemore series, it uses a WaveNet-style hierarchical MLP that progressively fuses character context in a tree structure rather than flattening all inputs at once. Trained on ~32,000 names with a full hyperparameter search pipeline.',
    challenge: 'The real difficulty was learning to see inside the network — understanding what healthy activations, gradient flow, and update ratios actually look like, and building the diagnostics tooling to check. An earlier RNN architecture had significant dead neuron problems that made this concrete: switching to WaveNet and LeakyReLU resolved it, but the work was in diagnosing why. The hyperparameter search across block size, hidden dim, embedding dim, and learning rate involved enough interacting variables that intuition alone wasn\'t reliable.',
    approach: 'WaveNet\'s hierarchical flatten-consecutive architecture outperformed both a flat MLP and a vanilla RNN. The key insight is that pairing adjacent representations at each layer builds meaningful bigram, 4-gram, and 8-gram features instead of treating all context positions as equally distant. Kaiming initialization kept pre-activation distributions healthy, and BatchNorm after each hierarchical layer stabilized training across the hyperparameter search.',
    features: [
      'Test loss of 1.97 cross-entropy on a held-out split of 32k names',
      'Hierarchical MLP: 8-char context window fused through bigram, 4-gram, and 8-gram layers',
      'Training diagnostics tracking update-to-data ratios, activation saturation, gradient flow, and dead neurons per layer',
      'Automated hyperparameter search across block size, hidden dim, and embedding dim with early stopping',
    ],
    links: [
      { label: 'GitHub', href: 'https://github.com/objones25/makemore' },
    ],
  },
  {
    slug: 'weather-api',
    title: 'Weather API',
    description: 'Async FastAPI wrapper around the Visual Crossing weather API with Redis caching, rate limiting, and SQLite request history.',
    year: '2026',
    tags: ['Python', 'FastAPI', 'Redis'],
    status: 'complete',
    overview: 'An async REST API built with FastAPI that wraps the Visual Crossing Timeline Weather API. Supports single and batch location lookups, Redis caching with configurable TTL, sliding window rate limiting, and SQLite request history. Built as a focused exercise in learning FastAPI and backend API design.',
    challenge: 'The hardest part was learning to read and apply framework documentation rather than leaning on AI to fill in gaps. FastAPI has a lot of moving parts — lifespan-managed clients, dependency injection via Depends(), middleware registration order, async context — and understanding why things work the way they do, not just copying patterns, was the actual goal of the project.',
    approach: 'FastAPI was chosen specifically to learn it. Redis made sense for caching given its native TTL support and the ability to implement the sliding window rate limiter using sorted sets in the same instance. All I/O is async throughout — httpx for HTTP, redis.asyncio for cache, aiosqlite for history — keeping the request path non-blocking end to end.',
    features: [
      '98 tests, 99% coverage; CI fails if coverage drops below 95%',
      'Cache hits served in ~100ms vs ~400ms from the upstream API',
      'Batch endpoint fans out up to 10 locations concurrently with asyncio.gather; partial failures return per-item errors',
      'Sliding window rate limiting via Redis sorted sets with X-RateLimit-* headers; batch requests consume N+1 units',
    ],
    links: [
      { label: 'GitHub', href: 'https://github.com/objones25/weather-api' },
    ],
    hasDemo: true,
  },
  {
    slug: 'fake-news-classifier',
    title: 'Fake News Classifier',
    description: 'ML pipeline for fake news detection using TF-IDF and ensemble methods. 99.97% test accuracy with AdaBoost on 38k articles.',
    year: '2026',
    tags: ['Python', 'scikit-learn', 'NLP'],
    status: 'complete',
    overview: 'A machine learning pipeline for classifying fake vs. real news articles, built as a hands-on introduction to scikit-learn and NLP. Covers the full workflow: EDA, data leakage detection, feature engineering, TF-IDF vectorization, feature selection, and hyperparameter-tuned comparison of 7 models on 38k articles.',
    challenge: 'The most important step was catching data leakage before touching a model. The subject column gave 100% classification accuracy on its own because real and fake news used completely non-overlapping subject labels — a model trained on that wouldn\'t be learning anything about language. The date column was a subtler version of the same problem. Removing both and building features purely from title and text content is what made the final accuracy meaningful.',
    approach: 'SelectKBest (f_classif) was chosen over PCA for dimensionality reduction. With sparse TF-IDF data, PCA required 13,075 components to retain 95% variance — more than double the 5,000 features SelectKBest needed, and 17x the disk footprint. AdaBoost won over XGBoost and LightGBM not because it\'s a better algorithm in general, but because the dataset is highly separable; the simpler ensemble was sufficient and trained in 4 minutes vs. 80 for Gradient Boosting.',
    features: [
      '99.97% test accuracy — 3 misclassifications out of 7,730 test articles',
      'Detected and removed two sources of data leakage (subject column: 100% accuracy; date column: 75%) before modeling',
      'Title capital ratio was the single most discriminative feature (F-score 45,372) — fake news averaged 30% caps vs. 6% for real',
      '7 models compared with RandomizedSearchCV; AdaBoost outperformed XGBoost, LightGBM, and Random Forest',
    ],
    links: [
      { label: 'GitHub', href: 'https://github.com/objones25/fake-news-classifier' },
    ],
    hasDemo: true,
  },
  {
    slug: 'computer-use-agent',
    title: 'Computer Use Agent',
    description: 'Autonomous desktop agent using Claude\'s Computer Use API with human-in-the-loop credential handling and CAPTCHA solving.',
    year: '2026',
    tags: ['Python', 'Claude API', 'Agents'],
    status: 'complete',
    overview: 'A Python agent that drives a containerized virtual desktop using Claude\'s Computer Use API. It can browse the web, fill forms, and handle full login flows — pausing to prompt the user for credentials and 2FA codes when needed, and falling back to CapMonster for CAPTCHA solving.',
    challenge: 'The credential flow was the hardest part to get right. The agent needs to know when to pause and ask for sensitive input, collect it securely (passwords hidden from output), then hand control back to Claude without losing context. Designing that as a first-class tool Claude could call — rather than pre-loading credentials or hardcoding intercept logic — made the flow composable and easier to reason about.',
    approach: 'The credential handler is implemented as a tool alongside the computer and bash tools, so Claude can request a username, password, or 2FA code the same way it would take a screenshot or run a command. Three configurable modes (always_confirm, sensitive_only, minimal) let you tune how much human oversight the agent requires without changing the underlying logic.',
    features: [
      'Full login flow support: username, password (hidden input), 2FA, and CAPTCHA — all handled without pre-storing credentials',
      'Three human-in-the-loop modes: always_confirm, sensitive_only, and minimal',
      'CapMonster integration for automated reCAPTCHA v2/v3, Cloudflare Turnstile, and AWS WAF solving',
      'Containerized virtual desktop via Docker, Xvfb, and Firefox ESR with optional VNC for live viewing',
    ],
    links: [
      { label: 'GitHub', href: 'https://github.com/objones25/computer-use-agent' },
    ],
  },
  {
    slug: 'neural-network-library',
    title: 'Neural Network Library',
    description: 'Neural network framework built from scratch in NumPy with full backprop, 8 activation functions, 5 optimizers, and 373 tests.',
    year: '2025',
    tags: ['Python', 'NumPy', 'ML'],
    status: 'complete',
    overview: 'A NumPy-only neural network library built from scratch as a learning exercise in understanding what frameworks like PyTorch abstract away. Supports dense layers, 8 activation functions, 5 optimizers including Adam, L1/L2/Dropout regularization, and multiple cost functions for regression and classification.',
    challenge: 'Implementing backpropagation and the optimizer update rules by hand — getting the gradient flow correct through each layer, handling the chain rule for different activation functions, and making sure batch operations vectorized properly. The implementation draws heavily from GeeksForGeeks and CS231n material; the value was in working through the math rather than original design.',
    approach: 'NumPy only, by design — the goal was to understand how the operations work, not to build something production-ready. Each component (activation, optimizer, cost function, initializer) is independent and interchangeable behind a common interface, following scikit-learn\'s API conventions. A four-tier validation system catches shape mismatches and NaN/Inf values early so errors surface at configuration time rather than mid-training.',
    features: [
      '373 unit tests across all components — activations, optimizers, cost functions, regularization, validation',
      'Five optimizers: SGD, Momentum, RMSprop, AdaGrad, and Adam',
      'Xavier and He initialization with matching activation function guidance',
      'Dropout and L1/L2/ElasticNet regularization with per-layer configuration',
    ],
    links: [
      { label: 'GitHub', href: 'https://github.com/objones25/neural-network-from-scratch' },
    ],
  },
  {
    slug: 'document-scanner',
    title: 'Document Scanner',
    description: 'Python CLI that extracts text from images, PDFs, and web pages and lets you have streaming AI conversations about the content.',
    year: '2025',
    tags: ['Python', 'OCR', 'AI'],
    status: 'complete',
    overview: 'A command-line tool for extracting text from images, PDFs, DOCX files, and URLs, then having multi-turn conversations about the content via streaming AI responses. Supports Tesseract for local OCR and Mistral OCR for handwritten or complex layouts, with Anthropic, OpenAI, and Gemini as AI backends.',
    challenge: 'Getting reliable text extraction across document types required more preprocessing than expected — images destined for Tesseract need enhancement passes before OCR quality is usable, and PDFs that appear text-based are often scanned, requiring a fallback detection step. Wiring multi-provider streaming into a consistent interactive interface also meant handling meaningfully different SSE implementations across three APIs.',
    approach: 'Tesseract handles printed text well locally at no cost; Mistral OCR covers handwriting and messy layouts where Tesseract struggles. The image preprocessing pipeline runs before whichever OCR engine is chosen, so the quality improvement applies to both. Each AI provider is wrapped behind a common interface so the conversation loop doesn\'t need to know which backend is active.',
    features: [
      '135+ tests across extraction, OCR, preprocessing, and summarizer modules',
      'Dual OCR engines: Tesseract (local) and Mistral OCR (API) with automatic image preprocessing',
      'Streaming multi-turn conversation interface with full history across Anthropic, OpenAI, and Gemini',
      'PDF text extraction with OCR fallback for scanned documents',
    ],
    links: [
      { label: 'GitHub', href: 'https://github.com/objones25/document-scanner-summarizer' },
    ],
  },
  {
    slug: 'browserbase-agent',
    title: 'Browserbase Agent',
    description: 'Cloudflare Worker exposing browser automation as a REST API, powered by Stagehand and Cloudflare Browser Rendering.',
    year: '2025',
    tags: ['TypeScript', 'Cloudflare', 'Agents'],
    status: 'complete',
    overview: 'A Cloudflare Worker that wraps Stagehand and Cloudflare Browser Rendering into a simple REST API. Send a natural language prompt and a URL, get back a full action history. Supports multiple LLM providers, SSE streaming, MCP integrations, and secure variable substitution for credentials.',
    challenge: 'Mostly a docs-following exercise in deploying an agent on Cloudflare Workers and learning how Stagehand abstracts browser automation. The variable substitution pattern — where sensitive values are swapped in client-side and never sent to the LLM — was the most interesting design detail to get right.',
    approach: 'Built on Cloudflare Browser Rendering rather than a self-hosted browser to keep the infrastructure serverless. Stagehand handles translating natural language actions into Playwright calls, so the worker itself stays thin — just request parsing, variable substitution, and streaming the action log back over SSE.',
    features: [
      'Natural language browser automation via a single POST endpoint',
      'Secure variable substitution — credentials are injected after the prompt is sent to the LLM',
      'SSE streaming for real-time action updates',
      'MCP integration support for external tools (search, docs, databases)',
    ],
    links: [
      { label: 'GitHub', href: 'https://github.com/objones25/browserbase-agent' },
    ],
  },
  {
    slug: 'brave-search-agent',
    title: 'Brave Search Agent',
    description: 'Cloudflare Worker API with four search modes — direct, AI-synthesized, query-optimized, and fully agentic — powered by Brave Search and Gemini.',
    year: '2025',
    tags: ['TypeScript', 'Cloudflare', 'Gemini'],
    status: 'active',
    overview: 'A Cloudflare Worker that wraps Brave Search through four progressively more intelligent endpoints. The simplest runs a direct web search. AI mode passes results to Gemini for synthesis. Optimized mode has Gemini rewrite and expand the query, fans out multiple searches in parallel, then merges and deduplicates results. Agentic mode gives Gemini full autonomy — it calls Brave Search as a tool up to five times, using Gemini 3\'s thought signatures to maintain reasoning context across turns, then synthesizes a cited answer.',
    challenge: 'Getting the agentic mode right required careful multi-turn function calling. Gemini 3 uses thought signatures to preserve reasoning context across sequential tool calls — preserving that state, feeding results back correctly, and deciding when to stop before the 5-call cap was the core design problem. The optimized mode needed query deduplication logic to avoid resurfacing the same result from alternative queries that converge on similar terms.',
    approach: 'Each mode is a separate endpoint so callers choose the cost/quality tradeoff explicitly. Agentic mode passes a Brave Search tool definition to Gemini and loops until it calls stop or hits the cap. Optimized mode generates alternatives in a single Gemini call, fans out with Promise.all, then re-ranks the merged result set. Standard and AI modes are stateless — one Brave call, optional Gemini synthesis, return.',
    features: [
      'Four endpoints: /search (direct), /ai (Gemini-synthesized), /optimized (query-expanded fan-out), /agentic (autonomous multi-step)',
      'Agentic mode: Gemini autonomously calls Brave Search up to 5 times using native function calling with thought signatures',
      'Optimized mode: generates 3-5 alternative queries in parallel and merges/deduplicates the result set',
      'Full Brave Search options: count, safesearch, freshness, result_filter, country, extra_snippets, and more',
    ],
    links: [
      { label: 'GitHub', href: 'https://github.com/objones25/brave-agent' },
    ],
    hasDemo: true,
  },
  {
    slug: 'battleship',
    title: 'Battleship',
    description: 'Real-time multiplayer Battleship with React, Socket.io, and JWT authentication via HTTP-only cookies.',
    year: '2025',
    tags: ['TypeScript', 'React', 'Socket.io'],
    status: 'complete',
    overview: 'A real-time multiplayer Battleship game with room-based matchmaking, ship placement, and turn-based gameplay over WebSockets. Built with React and TypeScript on the frontend, Node.js and Express on the backend, and Socket.io for bidirectional real-time communication.',
    challenge: 'Getting JWT authentication to work correctly with Socket.io was the main pain point. Storing tokens in HTTP-only cookies (to prevent XSS) means the socket connection can\'t read them from JavaScript directly — the cookie has to be parsed server-side from the socket handshake headers. Malformed token errors during the handshake were hard to trace, and getting the socket to automatically re-authenticate after login required explicit reconnection logic rather than relying on the default behavior.',
    approach: 'Socket.io handles all game state updates — ship placement, turn changes, hit/miss results, and disconnect events — while the REST API handles authentication and room management. HTTP-only cookies carry the JWT so credentials never touch client-side JavaScript. Automatic room cleanup runs on disconnect and timeout to prevent memory leaks from abandoned sessions.',
    features: [
      'Real-time turn-based gameplay over WebSockets with hit/miss/win events',
      'JWT authentication via HTTP-only cookies with automatic socket re-authentication after login',
      'Room-based matchmaking with automatic cleanup on disconnect and timeout',
      'Full ship placement phase with collision and boundary validation',
    ],
    links: [
      { label: 'GitHub', href: 'https://github.com/objones25/battleship-game' },
    ],
  },
  {
    slug: 'gemini-audio-agent',
    title: 'Gemini Audio Agent',
    description: 'Voice-enabled AI assistant on Cloudflare Workers with real-time WebSocket streaming, TTS, and Gemini thinking mode.',
    year: '2025',
    tags: ['TypeScript', 'Cloudflare', 'Gemini'],
    status: 'complete',
    overview: 'A voice-enabled AI assistant deployed as a Cloudflare Worker. Captures audio via WebRTC, streams it to Gemini for transcription and response, then streams TTS audio back — all over a persistent WebSocket connection. Supports Gemini 2.0 Flash for fast responses and 2.5 Flash for reasoning with visible thinking steps.',
    challenge: 'WebSocket streaming on Cloudflare Workers was the hardest part. Workers run in a request-response model — maintaining a persistent bidirectional connection while simultaneously streaming text chunks, thinking steps, and TTS audio chunks back to the client required careful handling of the Workers runtime\'s WebSocket support and the ordering of async operations across all three stream types.',
    approach: 'Hono handles HTTP routing while WebSocket upgrade requests are handed off to a persistent handler that manages the Gemini stream. Text, thinking, and audio chunks are typed and sequenced before being sent to the client so the frontend knows how to render each one. The two model options map to different capability sets: 2.0 Flash for low-latency responses with search, 2.5 Flash when the user wants to see the reasoning process.',
    features: [
      'Real-time voice input via WebRTC with automatic transcription through Gemini',
      'TTS audio streamed back over WebSocket in chunks for low perceived latency',
      'Thinking mode support on Gemini 2.5 Flash — reasoning steps rendered in real-time',
      'Google Search integration for up-to-date information during voice conversations',
    ],
    links: [
      { label: 'GitHub', href: 'https://github.com/objones25/gemini-audio-agent' },
    ],
    hasDemo: true,
  },
  {
    slug: 'chess',
    title: 'Chess',
    description: 'Command-line chess in Ruby with full rule enforcement, check/checkmate/stalemate detection, and save/load. Capstone project for The Odin Project Ruby curriculum.',
    year: '2025',
    tags: ['Ruby', 'OOP', 'RSpec'],
    status: 'complete',
    overview: 'A two-player command-line chess game built as the capstone project for The Odin Project\'s Ruby curriculum. Enforces all standard chess rules including castling, en passant, and pawn promotion, with check, checkmate, and stalemate detection and a JSON save/load system.',
    challenge: 'The special moves were the hardest to get right — castling requires tracking whether the king and rook have moved, en passant has a one-move window that has to be cleared each turn, and check detection has to run after every move to filter out any move that would leave the king in check. Getting all of those edge cases to interact correctly without breaking each other took most of the debugging time.',
    approach: 'Each piece class implements its own movement logic and inherits shared behavior from a base Piece class. The Board class handles move validation and check detection centrally rather than spreading that logic across pieces — any candidate move is tested by applying it temporarily and checking whether the king is in check in the resulting state. This made adding special moves easier since the validation logic didn\'t have to change.',
    features: [
      '205 RSpec tests covering all piece movements, special rules, and game-ending conditions',
      'Full special move support: castling, en passant, and pawn promotion',
      'Check, checkmate, and stalemate detection after every move',
      'JSON save/load system with timestamped files and graceful error handling',
    ],
    links: [
      { label: 'GitHub', href: 'https://github.com/objones25/chess' },
    ],
  },
]

export const featuredProjects = projects.slice(0, 4)
